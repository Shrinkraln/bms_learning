> last task:功能分析和分层设计
>
> now task:数据分析 分层收敛架构 变量命名
>
> next task:修改架构和变量规范

1. [L] 继续完成数据设计
2. [P] 修改了接口，变量命名规则，规范time_ms_t取代uint8_t，曾强可读性；但是可以在变量名前添加：prv私有，ex会被其他函数当参数传入，写在主程序中，宏定义配置系数；同时指定层次，ex就是在main()函数里面的变量，prva是app层，各bsp可用，prvb是bsp层，用于内部如计数器
3. [F] BATHI BATLO 由于16bit放不下14bt>>4的最大值，所以只取14bit>>2存放，取出后再>>2
4. [F] I2C用不上DMA：读取的寄存器地址非连续
5. [P] 对于寄存器相关位的值可以确定使用enum枚举规范范围
6. [P] if判断功能是否执行放在函数外部，可读性
7. [P] 使用常数指针强调参数的只读性，和通过指针修改参数值区分