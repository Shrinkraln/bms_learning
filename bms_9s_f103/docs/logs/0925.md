> 当前阶段：实现裸机开发的底层驱动包装 
>
> 下一阶段：裸机开发的基本功能实现，其中信号信号量等通过标志位或者队列的形式实现
1. [F] bootloader使用的串口不需要手动初始化，在自身的硬件写死的代码里面已经有配置；那么配置为？
2. [P] 修改cmakelist，但是官方更推荐显式列出源文件
```txt
if(NOT CMAKE_OBJCOPY)
    set(CMAKE_OBJCOPY ${TOOLCHAIN_PREFIX}objcopy)
endif()
if(NOT CMAKE_SIZE)
    set(CMAKE_SIZE ${TOOLCHAIN_PREFIX}size)
endif()

add_custom_command(TARGET ${CMAKE_PROJECT_NAME} POST_BUILD
    COMMAND ${CMAKE_OBJCOPY} -O ihex
        $<TARGET_FILE:${CMAKE_PROJECT_NAME}>
        ${CMAKE_PROJECT_NAME}.hex
    COMMAND ${CMAKE_OBJCOPY} -O binary
        $<TARGET_FILE:${CMAKE_PROJECT_NAME}>
        ${CMAKE_PROJECT_NAME}.bin
    COMMAND ${CMAKE_SIZE}
        $<TARGET_FILE:${CMAKE_PROJECT_NAME}>
    COMMENT "生成 .hex .bin 和内存报告..."
)
```
3. [P] 修改了cmakelist之后需要重新配置
```bash
cmake --preset Debug #有Debug和Release两个选项
cmake --build --preset Debug
```
4. [F] bootloader使用是生成.hex文件，通过FlyMCU等其他上位机烧录，不需要修改Keil里调试器的选项
4. [P] cubemx生成的freertos不兼容keil6，需要在portable中将gcc的arm3移植到RVSDx下
4. [P] 制定函数命名和变量命名规范
   4. 函数命名前缀添加返回值类型
   4. 对于模拟的flag 使用volitle关键字修饰
   4. 变量命名前缀添加变量类型
4. [P] 撰写功能分析和分层设计 <a>../application_and_interficial.md</a>目的是提供框架，以及一些功能复用；添加数据类型设计
4. [F] 裸机先实现demo和底层调动能运行
4. [F] bq 会根据需要连接的电池数量有确定的引脚连接方式
4. [F] bq 电流保护和计算都是直接查表，电压保护和计算是需要分别gain+offset和反推adc值写入；保护自身触发有delay防误触；sys_sta 的状态只能是host清0，不能置1

